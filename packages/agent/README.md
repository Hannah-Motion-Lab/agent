# hannah-agent (main package)

The CLI/server entry point of the hannah-agent engine — forked from opencode's main
package (`packages/opencode`). See the repository root README for provenance,
renames, and usage.
